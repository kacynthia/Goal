import { StrickethrougthDirective } from './strickethrougth.directive';

describe('StrickethrougthDirective', () => {
  it('should create an instance', () => {
    const directive = new StrickethrougthDirective();
    expect(directive).toBeTruthy();
  });
});
